{
  "$schema": "./node_modules/turbo/schema.json",
  "globalDependencies": ["package.json", "tsconfig.json", "pnpm-lock.yaml", "pnpm-workspace.yaml"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "test": {
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "clean": {
      "outputs": []
    }
  }
}
